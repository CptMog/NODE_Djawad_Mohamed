.user_card
    h4 #{name.first} #{name.last}
        small (#{age} ans)
    p Né le 18/04/1986
    p Vit à
        strong #{location.city}, #{location.zipcode}
    if isAdmin
        span.badge-admin Est administrateur
    else
        span.badge-admin Est pas administrateur